<Window x:Class="Motor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Motor.ViewModel"
        xmlns:ctr="clr-namespace:Motor.Controls"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <Grid>
        <ctr:MotorControl Width="100" Height="100" 
                          DeviceName="{Binding Motor.DeviceName}" Connection="{Binding Motor.Connection}"
                          Running="{Binding Motor.Running}" Standby="{Binding Motor.Standby}" Fault="{Binding Motor.Fault}"/>

        <Button Content="运行" Command="{Binding RunningCmd}" Width="100" Height="60" Margin="242,291,458,84" />
        <Button Content="待机" Command="{Binding StandbyCmd}" Width="100" Height="60" Margin="483,291,217,84" />
        <Button Content="故障" Command="{Binding FaultCmd}" Width="100" Height="60" Margin="365,291,335,84" />
    </Grid>
</Window>
